import styled from "@emotion/styled";
import { css } from "@emotion/react";

export const Wrapper = styled('div')(
    css({
        height: '100vh',
        width: '100%',
        overflowX: 'hidden',
        overflowY: 'hidden',
    })
);