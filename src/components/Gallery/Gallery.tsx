import React from "react";
import AppWrapper from "../AppWrapper/AppWrapper";

const Gallery = () => (
    <AppWrapper>
        <div>Past event photos</div>
    </AppWrapper>
);

export default Gallery;