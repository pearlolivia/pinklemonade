import React from "react";
import AppWrapper from "../AppWrapper/AppWrapper";

const Contact = () => (
    <AppWrapper>
        <div>Contact Us</div>
    </AppWrapper>
);

export default Contact;
